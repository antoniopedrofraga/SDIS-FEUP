package database;

import java.util.HashMap;

import messages.Header;


public class Database {
	HashMap<Header, Header> storeConfirmations;
	
	public Database() {
		storeConfirmations = new HashMap<Header, Header>();
	}

	public HashMap<String, Long> getStoreConfirmations() {
		return storeConfirmations;
	}

	public int countConfirmedChunks(Header header) {
		int count = 0;
		Long chunkNo = Long.parseLong(header.getChunkNo());
		String fileId = header.getFileId();
		storeConfirmations.get(<fileId, chunkNo>);
		return count;
	};
}
