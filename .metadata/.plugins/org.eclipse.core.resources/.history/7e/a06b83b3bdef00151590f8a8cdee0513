package peers;

import java.net.MulticastSocket;

import exceptions.ArgsException;

public class Peer {
	private String serverId;
	
	private MulticastSocket mc;
	private int mcPort;
	
	private MulticastSocket mdb;
	private int mdbPort;
	
	private MulticastSocket mdr;
	private int mdrPort;
	
	public Peer(String serverId, String mcAddress, String mcPort,
			String mdbAddress, String mdbPort, String mdrAdress,
			String mdrPort) {
		
	}

	public static void main(String[] args) throws ArgsException {
		if (args.length != 7)
			throw new ArgsException("peer <Server ID> <MC> <MC port> <MDB> <MDB port> <MDR> <MDR port>");
		
		Peer peer = new Peer(args[0], args[1], args[2], args[3], args[4], args[5], args[6]);
	}
}
