package database;

import java.io.File;

import utilities.Constants;

public class Storage {
	Storage () {
		File files = new File(Constants.FILES_ROOT);
		if (!files.exists())
			files.mkdirs();
	}
}
