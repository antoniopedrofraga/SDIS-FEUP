package peers;

import exceptions.ArgsException;
import subprotocols.ChunkBackup;
import subprotocols.SubProtocol;
import utilities.Constants;

public interface InitiatorPeer {
	
	public static void main(String[] args) throws ArgsException {
		
		InitiatorPeer initiatorPeer = getPeerFromArgs(args);

		
	}

	public static InitiatorPeer getPeerFromArgs(String[] args);

	public static boolean getPeerFromArgs(String[] args) {
		
		String action = args[0];
		SubProtocol protocol;
		
		switch(action) {
		case Constants.BACKUP:
			if (args.length != 5)
				return false;
			
			protocol = new ChunkBackup(action, args[1], args[2], args[3], args[4]);
			break;
		default:
			return false;
		}
		
		return true;
	}
}
